# 2026-04-11_week1
**Completed:** Week 1 — Foundation & Wiring

## Changes

### Milestone 1 — Auth Wiring
- Enabled `beforeEnter: redirectIfAuthenticated` on `/login` and `/register` routes
- Enabled `beforeEnter: requireAuth` on all protected routes:
  - Dashboard (`/`)
  - Students Module (`/students`)
  - Payer Module (`/payer`)
  - Classes Module (`/classes`)
  - Accounting Module (`/accounting`)
- Fixed `requireAuth` and `redirectIfAuthenticated` guards to use `isAuthenticated` getter and `checkAuth()` method
- Verified `logout()` in auth store clears token and navigates to `/login` (handled in Header.vue)
- Verified 401 interceptor in `services/api.ts` clears auth and redirects to login

### Milestone 2 — Shell & Navigation
- Created `pages/dashboard/Index.vue` with:
  - Welcome card with navigation links
  - 4 stat placeholder cards (Total Students, Total Payers, Transactions, Recent Deposits)
  - Quick Actions section with links to common tasks
- Sidebar nav items already wired to correct named routes
- `AuthLayout.vue` renders Header + Sidebar + MainContent for all protected routes
- Confirmed `.env.development` has correct API base URL (`http://localhost:8000/api/v1`)

## Notes / blockers encountered
- The `isAuthenticated` in auth store is a getter (Boolean), not a method - updated route guards accordingly
- All UI components are already available in `components/ui/`
- Login and Register pages were already implemented and working with the API
